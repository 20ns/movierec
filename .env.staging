# Staging Environment Configuration
ENVIRONMENT=staging
NODE_ENV=production

# API Configuration (to be replaced by CI/CD)
REACT_APP_API_GATEWAY_INVOKE_URL=https://staging-api-gateway-url.amazonaws.com/prod/
REACT_APP_ENVIRONMENT=staging

# These will be injected by CI/CD from GitHub secrets
REACT_APP_TMDB_API_KEY=staging_tmdb_api_key
REACT_APP_COGNITO_USER_POOL_ID=staging_user_pool_id
REACT_APP_COGNITO_CLIENT_ID=staging_client_id
REACT_APP_COGNITO_REGION=eu-north-1

# AWS Configuration
AWS_REGION=eu-north-1

# Health Check Configuration
HEALTH_CHECK_URL=https://staging.movierec.net

# AWS Amplify Configuration
AMPLIFY_BRANCH=develop
AMPLIFY_ENVIRONMENT=staging