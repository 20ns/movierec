// This file is no longer needed with Amplify v6
// Configuration is now handled in the main Amplify.configure() call

const configureAuth = () => {
  // Auth configuration is now handled by the main Amplify configuration
  console.log('Auth configuration is handled by main Amplify.configure()');
};

export default configureAuth;