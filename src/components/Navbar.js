import React from 'react';
import SearchBar from './SearchBar';

const Navbar = () => {
  return (
    <nav>
      <SearchBar />
      {/* Navbar content */}
    </nav>
  );
};

export default Navbar;
